[package]
name = "goosedb"
version = "0.1.0"
edition = "2021"

[dependencies]
arrow = "58"
parquet = { version = "58", features = ["arrow"] }
csv = "1"
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
